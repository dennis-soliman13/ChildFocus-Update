<?xml version="1.0" encoding="utf-8"?>
<LinearLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="#EE000000">

    <!-- Block message header -->
    <TextView
        android:id="@+id/tvBlocked"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Content Blocked"
        android:textSize="22sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:gravity="center"/>

    <TextView
        android:id="@+id/tvReason"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="This video was flagged as overstimulating."
        android:textSize="14sp"
        android:textColor="#CCFFFFFF"
        android:gravity="center"/>

    <TextView
        android:id="@+id/tvScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text=""
        android:textSize="12sp"
        android:textColor="#99FFFFFF"
        android:gravity="center"/>

    <!-- Safe suggestions section -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:layout_marginBottom="12dp"
        android:text="Watch something better instead:"
        android:textSize="13sp"
        android:textColor="#CCFFFFFF"/>

    <!-- Container for suggestion cards (populated programmatically) -->
    <LinearLayout
        android:id="@+id/llSuggestions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"/>

    <!-- Loading spinner while fetching suggestions -->
    <ProgressBar
        android:id="@+id/pbSuggestions"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:visibility="visible"/>

    <Button
        android:id="@+id/btnClose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Dismiss"
        android:backgroundTint="#44FFFFFF"
        android:textColor="#FFFFFF"/>

</LinearLayout>
